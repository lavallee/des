name: Deploy DES documentation

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/pages.yml"
      - "assets/**"
      - "modes/**"
      - "scripts/des-profile.mjs"
      - "themes/**"
      - "ui-kits/**"
      - "website/**"
      - "colors_and_type.css"
      - "components.css"
      - "package.json"
      - "showcase.html"
      - "tokens.css"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Build and test
        run: npm test
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Upload site
        uses: actions/upload-pages-artifact@v3
        with:
          path: website/site
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
